# Server bootstrap (copy to the deploy directory as .env).
# Deploy is via GitHub Actions on push to main — not from this machine.

TELEGRAM_BOT_TOKEN=

IMAGE=ghcr.io/crearec/crea-flibusta-bot
IMAGE_TAG=main
